import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';import{STUDENTS,buildRegressionTree,predict}from'../lessons/decision-tree-score/game-core.js';test('회귀 트리는 유효한 점수를 예측하고 질문 경로를 반환한다',()=>{const r=predict(buildRegressionTree(STUDENTS,3),4);assert.ok(r.score>=0&&r.score<=100);assert.ok(r.path.length>0)});test('화면은 공부 시간으로 시험 점수를 예측하는 활동임을 분명히 밝힌다',async()=>{const h=await readFile(new URL('../lessons/decision-tree-score/index.html',import.meta.url),'utf8');assert.match(h,/공부 시간으로<br>시험 점수 예측하기/);assert.match(h,/의사결정트리/);assert.match(h,/data-guard-group="machine-learning-algorithms"/)})
